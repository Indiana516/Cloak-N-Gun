function outputScores(rank, score, name)
{
  document.write('<tr>');
  document.write('<td>' + rank + '</td>');
  document.write('<td>' + score + '</td>');
  document.write('<td>' + name + '</td>');
  document.write('</tr>');
}
